<!DOCTYPE html>
<html lang="en">
<%- include("head")%>
<body>
    <%- include("header")%>
   
    <% if(users.length >0){%>
        
            <% users.forEach(function(user){%>
               <div class="home"> 
                
                    <h3><%= user.name %>-      <%= user.createdAt %></h3> 
                <p><%= user. message  %></p>
                <% });%>
               
                
               </div>
        

    <%} %>
</body>
</html>